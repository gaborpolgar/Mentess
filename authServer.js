/* require("dotenv").config();

const express = require('express');
const app = express();

app.use(express.json());

const port = process.env.TOKEN_SERVER_PORT;
console.log("A kapcsolat megkísérlése");
 */